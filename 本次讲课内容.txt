io sql
namespace="" 默认命名空间
/ 根命名空间
/sub
1 都可以访问自己下的action和默认下的action
2 互相不能访问
action的全名:命名空间+action名字

<include file=""/>
struts ((package|include|bean|constant)*, 
|不分前后，*代表0或多个
unknown-handler-stack?)>
？代表0或1个

访问网站要么在根目录
/default.action

要么在子目录
/sub/sub.action

""有default,/下有root(/root.action),
/sub下有sub(/sub/sub.action)
可以访问：/default.action,/sub/default.action
不能互相访问/sub.action,/sub/root.action

<s:url var="sub2Root" action="root" namespace="/sub"/>
    <a href="${sub2Root}">/sub访问/下action</a> 