action类名-validation.xml
<field name="test_int">
  <field-validator type="int">
    <param name="min">20</param>
    <param name="max">50</param>
    <message>超出范围[${min},${max}]</message>
  </field-validator>
</field>
message key=
double
minInclusive include
maxInclusive

stringlength什么也不输，不会校验
required校验对象 null ""
requiredstring
fieldexpression  pass.equals(repass)

<field name="pass">
	   <field-validator type="fieldexpression">
	    <param name="expression">pass.equals(repass)</param>
	    <message>两次输入密码不一致</message>
	     
	   </field-validator>
	</field>
	(20,50) test_int<50&&test_int>20
	
	
	<!CDATA[[  ]]>
	
	expression：表达式校验器，
	唯一的非字段校验器，	
	错误信息在actionerror中
	